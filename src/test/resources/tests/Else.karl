use std::io;

declare int: number = 10;
if (number == 11) -> {
    Show("No");
} else if (number == 10) -> {
    Show("Test #1 success");
}

if (number < 10) -> {
    Show("No");
} else -> {
    Show("Test #2 success");
}

declare func main::(): void -> {
    if (!true) -> {
        Show("No");
    } else -> {
        Show("Test #3 success");
    }
}

main();

declare bool: testPassed = false;
if (testPassed) -> {
    Show("No");
} else -> {
     testPassed = true;
}

if (testPassed) -> {
    Show("Test #4 success");
}