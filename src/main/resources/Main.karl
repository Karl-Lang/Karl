int: number = 0;
if (10 > 1 && (false == false)) -> {
    show(number);
    show("Hello World!");
} else -> {
    number = number + 1;
    show(number, " output");
    show("Goodbye World!");
}

show("Number is ", number);