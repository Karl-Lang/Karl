string: name = "Karl";
int: number = 230;
float: price = 2.30;
bool: boolean = true;
bool: trueValue = true;

if ((name == "karl" || name == "Karl") && (number == 230 || number == 231)) -> {
    show("Test #10 success");
}

func getBool::(): bool -> {
    show(number);
    return false;
}

if (!getBool()) -> {
    show("Test #8 success");
}