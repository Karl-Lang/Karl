string: name = "Karl";
int: number = 230;
float: price = 2.30;
bool: boolean = false;
bool: trueValue = true;

if (price == 2.30 && (!boolean || number > 1300)) -> {
    show("Test #3 success");
} else -> {
    show("Test #3 failed");
}

func getBool::(): bool -> {
    return false;
}

if (!boolean) -> {
    show("Test #8 success");
}