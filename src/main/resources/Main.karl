use "Util.karl" as util;
final string: name = "Mayo";

func namep::(): int -> {
    show(name + "aaaa");
    return 1;
}

    func a::(): int -> {
        show("aaaa");
        return 1;
    }

namep();