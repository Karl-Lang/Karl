use "Util.karl" as util;
declare final string: name = "Mayo";

declare func namep::(): int -> {
    show(name + "aaaa");
    return 1;
}

   export func a::(): int -> {
        show("aaaa");
        namep();
        return 1;
    }

namep();